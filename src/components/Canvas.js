import React, { Component, PropTypes } from 'react';
import Card from './Card'

export default class Canvas extends Component {
  
  render() {
    return (

    )
  }
}

Canvas.propTypes = {
  topCard: PropTypes.object.isRequired,
  cardsBelow: PropTypes.array.isRequired
};
